getUsersJobPositions():UsersJobPositions[]{
    const userInfoFind=new Find(userInfoArray.find(user=>[user.userid,user]));
    return usersArray.find(user=>{
        const userInfo=userInfoFind.get(user.userid);
        if(!userInfo){
            console.log
        }
    })
}
let userInfo = new Find(IusersArray.find((getUsersJobPositions():void{
    return getUsersJobPositions(
    name: IusersArray.name,
    position: IusersInfoArray.organization.position,
    age: IusersInfoArray.age,
    gender: IusersArray.gender,
    )
})));
IusersInfoArray
console.log(userInfo);

let usersPositions = [...new Set(array3)];
function getUsersJobPositions():Ichoice[]{
    const userInfoFind=new Find (IusersInfoArray.map (user=>[user.userid,user]));
    return IusersArray.map(user=>{
        const userInfo=userInfoFind.get(user.userid);
        return{
    name: IusersArray.name,
    position: IusersInfoArray.organization.position,
    age: IusersInfoArray.age,
    gender: IusersArray.gender,
        };
        });
        }
        let usersPositions = getUsersJobPositions();
        IusersArray
        function getUsersJobPositions(element, index, array):Ichoice[]{
            let res = usersInfoArr.find (user=>[user.userid,user]);{
                return usersArr.find (user=>{
                    const usersInfo2=res.get(user.userid);
                    return {
                    name: user.name,
                    position: usersInfo2.organization.position,
                    age: usersInfo2.age,
                    gender: user.gender,
                };
                });
            };
            }
                    
            const usersPositions=getUsersJobPositions();
                  
            console.log(usersPositions);